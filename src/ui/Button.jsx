const Button = ({ title }) => {
  return (
    <button className="text-lg bg-yellow-300 py-4 px-11 rounded-[40px] h-14 w-[232px] flex-none">
      {title}
    </button>
  );
};

export default Button;
